---
import {
  Content,
  frontmatter,
} from "../../BeneficiosMD/BeneficiosFisioterapia.md";
---

<div class="container">
  <article>
    <h2>{frontmatter.title}</h2>
    <Content />
  </article>
  <img src="img/BeneficiosFisioterapia.jpg" alt="" />
</div>

<style>
  .container {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem;
    margin: 4rem 0;
    border-bottom: 2px solid var(--color-primary);
    padding: 2rem;
  }

  article {
    padding: 1rem;
    background: #f8fafc;
    border-radius: 8px;
  }

  h2 {
    color: var(--color-primary);
    margin-bottom: 1rem;
    font-size: 1.5rem;
  }

  p {
    color: var(--color-text-main);
    margin-bottom: 0.5rem;
    font-size: 1rem;
  }

  img {
    border-radius: 8px;
    width: 100%;
    height: auto;
  }

  @media (max-width: 768px) {
    div {
      grid-template-columns: 1fr;
    }
  }
</style>
